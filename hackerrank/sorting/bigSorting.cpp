#include <iostream>
#include <vector>
#include <string>

int main() {

	// input first row (number of big ints
	unsigned int num;
	(std::cin >> num).get();

	// input the rest
	std::vector<std::string> sortedBigNum(num);
	for (std::string &numString : sortedBigNum) {

		std::string tempNum;
		std::getline(std::cin, tempNum);


		
	}

}
